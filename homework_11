"""Опишите как могла бы работать функция superset() используя только if else и операции над множествами.
а.superset(с)-все элементы с входят в а, тогда true
множество хранит уникальные элементы
Использовать встроенную функцию issuperset нельзя!/"""
d = {1, 2, 3, 4, 5, 7, 8, 15}  # список 1
c = {1, 2, 3, 8}  # список 2
# z = d.intersection(c)  # список общих элементов
r = d | c
if len(r) == len(d):
    print('Список с является суперсетом списка d')
else:
    print('Список с не является суперсетом списка d')

