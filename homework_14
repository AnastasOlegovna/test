"""Функция arithmetic, принимающая 3 аргумента: первые 2 - числа,
третий - операция, которая должна быть произведена над ними.
Функция должна вернуть результат вычислений зависящий от третьего аргумент +,сложить их; если —, то вычесть; * — умножить; / — разделить (первое на второе).
В остальных случаях вернуть строку "Неизвестная операция"""
x = 7
y = 14
def arithmetic(x, y, operation):
    if operation == x + y:
        print('результат:', operation)
    elif operation == x - y:
        print('результат:', operation)
    elif operation == x * y:
        print('результат:', operation)
    elif operation == x / y:
        print('результат:', operation)
    else:
        print('Неизвестная операция')

arithmetic(x, y, x * y)
