# Дан список чисел.
# Определите, сколько в этом списке элементов, которые больше двух своих соседей (слева и справа)
# выведите количество таких элементов. Первое и последнее значение не учитывать.
a = [15, 30, 15, 140, 120, 100, 110, 109, 200, 1000]
count = 0   # вводим 0 для нашего счетчика чисел(count), которые больше соседей
x, y = 0, 2     # индексы соседей чисел в списке(при первом проходе цикла for)

for i in a[1:-1]:   # срез убирает 0 и последнее значение списка
    if i > a[x] and i > a[y]:
        count += 1
    x += 1
    y += 1
print(count)    # печатаем кол-во элементов, которые больше соседей
