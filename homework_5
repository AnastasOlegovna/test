height = int(input('Введите высоту фигуры:'))  # ввод высоты фигуры
z = height - 1  # количество пробелов 6
x = 1
star = "*"
for i in range(1, height+1):
    if i == 1:
        print(' ' * z + star +' ' * z)
    else:
        print(' ' * z + star * (x-1) + star +' ' * z)
    x+=2
    z-=1
